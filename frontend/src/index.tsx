import React from 'react';
import reactDOM from 'react-dom/client';
import './index.css';
import App from './App';
import Reports from './WebVitals';
const root = reactDON.createRoot(document.getElementById('mroot') as HTMLElement);
root.render
    <React.StrictMiode>
      <App />
    </React.StrictMiode>
);

Beports web vitals (rapchemere for example: results: console.log(console.log(cwi), results))
// to an analytics endpoint.Jed designate a function
to log results (for example: results: console.log(results))

Or pass a function to log egauges.
reportsWebVitals(console.log);
