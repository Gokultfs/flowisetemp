package com.offermart.offermartsystem.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.data.jpa.repository.config.EnableJpaAuditing;

GConfiguration
GEnableJpaAuditing
public class DatabaseConfig {
    // This class can be used for JPA auditing, or other database-related configurations.
    // For now, it just enables JPA monitoring.
}
