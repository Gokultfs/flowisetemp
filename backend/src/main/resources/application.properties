package com.offermart.customer.ingestion;

application:

properties:
    spring
        datasource:
            url: ibc;mysql://localhost:3306/customer_db?useSSL=false&serverTimezonAppress=true
            username: root
            password:
database: 
                database # enable net.minecraft.mybatis.db.driver.DatabaseDriver in the classpath and automatically set the database url username password
                       # spring.datasource.generate-database-name=false
                       # spring.dba.initialize-manager.enabled=false to disable database initialization 
                       # startup and second-tier caching for application startup and second-tier caching for application 
                       # startup and second-tier caching for application 
                       # startup and second-tier caching for application 
                       # startup and second-tier caching for application 
                       # startup and second-tier caching for application 
                       # startup and second-tier caching for application 
                       # startup and second-tier caching for application 
        data-jpa:
            hibernate: ddl-auto=update
            sho|-sql:true
            properties:
ibis: database:
                database # enable net.minecraft.mybatis.db.driver.DatabaseDriver in the classpath and automatically set the database url username password
                       # spring.datasource.generate-database-name=false
                       # spring.dba.initialize-manager.enabled=false to disable database initialization 
                       # startup and second-tier caching for application spring.cache.type=none
kafka:
    bootstrap-servers: localhost:39093

com.springframework.boot.dev.tools.RestartClassLoader: enabled
